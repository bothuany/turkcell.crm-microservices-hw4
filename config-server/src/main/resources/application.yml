spring:
  application:
    name: "config-server"
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/bothuany/turkcell.crm-microservices-hw4-configs.git"
          default-label: main
server:
  port: 8079

management:
  endpoints:
    web:
      exposure:
        include: "*"
  enpoint:
    health:
      show-details: always