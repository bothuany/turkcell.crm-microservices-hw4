apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: comturkcellcrm-microservices-hw4-product-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8130
          envFrom:
            - configMapRef:
                name: product-service-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
