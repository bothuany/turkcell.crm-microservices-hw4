apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: microservices
spec:
  selector:
    app: identity-service
  ports:
    - port: 8100
      targetPort: 8100
  type: ClusterIP
