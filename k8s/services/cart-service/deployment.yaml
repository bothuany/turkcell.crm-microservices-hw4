apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
        - name: cart-service
          image: comturkcellcrm-microservices-hw4-cart-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8140
          envFrom:
            - configMapRef:
                name: cart-service-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
