apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: comturkcellcrm-microservices-hw4-order-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8120
          envFrom:
            - configMapRef:
                name: order-service-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
